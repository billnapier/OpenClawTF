# Quickstart: Disaster Recovery Restoration

## Execution Commands

Execute dry-run disaster recovery verification:

```bash
bash scripts/disaster_recovery_restore.sh --dry-run
```

Run test suite:

```bash
bash scripts/test_disaster_recovery.sh
```
